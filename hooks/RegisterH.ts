// import { useQuery } from '@tanstack/react-query';
// import { Register } from '@/app/api/Register';

// // Define the custom hook
// const useStudentQuery = (studentId: string, firstName: string) => {
//     return useQuery({
//         queryKey: ['students', studentId, firstName],
//         queryFn: () => Register(studentId, firstName),
//         // Optionally, you can configure more options like staleTime, cacheTime, etc.
//     });
// };

// export default useStudentQuery;
